# Before state

Captured in git, not pixels: the prior /journal surface was the AM/PM
PersonalPracticeSurface (see `src/components/personal-practice-surfaces.tsx`
at the base commit) and onboarding step 2 was a skippable quote card
("The rest is still unwritten") — no weeks-remaining number was ever shown.
